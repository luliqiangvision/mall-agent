strict-peer-dependencies=false    # 不严格检查 peer dependencies
auto-install-peers=true           # 自动安装 peer dependencies
shamefully-hoist=true             # 将所有依赖提升到根目录（扁平化结构）
ignore-scripts=false              # 执行 npm scripts（postinstall 等）
enable-pre-post-scripts=true      # 启用 pre/post 脚本

